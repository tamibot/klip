# Claude Code: personal local config (not shared in git)
.claude/settings.local.json

# macOS
.DS_Store

# Swift / SwiftPM
.build/
.swiftpm/
# Package.resolved is committed to pin dependency versions (WhisperKit etc.) for reproducible builds.

# Build artifact (produced by ./build.sh)
*.app/

# Locally installed skills (npx skills) — not part of the product
.agents/
.claude/skills/
skills-lock.json

# Local security-audit reports (gstack) — stay on the machine, never pushed
.gstack/
